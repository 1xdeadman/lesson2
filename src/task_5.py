"""
Внимательно прочитайте код в этом файле и добавьте свой код везде, где встречается <YOUR CODE>.
Перед каждым заданием вы можете прочитать его описание, помеченное меткой TODO.
для проверки введите из папки с проектом: pytest -m test_task_5
"""
from typing import Any, Union


def add_new_elem(container: list, new_data: Any) -> None:
    # TODO: добавьте в конец списока container новый элемент со значением переменной new_data
    "<YOUR CODE>"


def change_elem(container: list, index: int, new_data: Any) -> None:
    # TODO: измените в списке container элемент под индексом index на значение переменной new_data
    "<YOUR CODE>"


def delete_elem(container: list, index: int) -> None:
    # TODO: удалите из списка container элемент под индексом index. Размер списка после удаления
    #  уменьшается на 1. После удаления по индексу index будет находиться элемент, ранее находившийся
    #  на позиции index + 1
    "<YOUR CODE>"


def get_sub_dict_elem(container: dict, key: str, subkey: Union[str, int]) -> int:
    # TODO: получите из подсловаря словаря container по ключу key элемент по ключу subkey.
    #  Запишите его в переменную result
    result = "you are not prepared!"

    result = "<YOUR CODE>"

    return result


